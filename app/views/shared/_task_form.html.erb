
	<%= form_with(model: @task, local: true) do |f| %>
	<%= render "shared/error_messages", object: f.object %>

	<%= f.label :content %>
	<%= f.text_field :content, class: "task-controll" %>

	<%= f.submit "create task", class: "btn btn-primary" %>

	<% end %>
